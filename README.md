# URL Shortener

A simple URL Shortener API built with Node.js, Express, and SQLite.

## Features

- Create short URLs
- Redirect shortened URLs
- Store URLs in SQLite database

## Installation

```bash
npm install
